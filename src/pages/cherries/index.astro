---
import {PAGE_SIZE} from "../../consts.ts"
import getEntries from "../../entries.ts"
const length = (await getEntries()).length
const latest = Math.ceil(length / PAGE_SIZE)
return Astro.rewrite(`/cherries/page/${latest}`)
---
